# Frozen Configuration: Phase 5B Extended Micro-Pilot Baseline
# Status: IMMUTABLE SPECIFICATION FOR PHASE 6A AUTONOMOUS GOVERNANCE VALIDATION
# Created: Phase 5B Completion & Verification
# Purpose: Exact frozen reference for Phase 6A Autonomous Execution & Autonomy Gap Validation

phase: "5B"
champion_name: "CHAMPION_AUTONOMOUS_MICRO_MODEL"
freeze_timestamp: "2026-09-15T14:30:00Z"
verdict: "EXTENDED_MICRO_VALIDATED"
git_commit_hash: "ac4b2e5a7b8e1f0c3d9a6b5c2d4e8f0a1b2c3d4e"

execution_mode: "LIVE_AUTONOMOUS_MICRO" # Permitted: SHADOW, BROKER_PAPER, LIVE_GOVERNED_MICRO, LIVE_AUTONOMOUS_MICRO. (LIVE remains prohibited)

pilot_specification:
  pilot_name: "PHASE_6A_AUTONOMOUS_GOVERNANCE_PILOT"
  max_live_capital_usd: 1000.0   # Absolute hard ceiling ($1,000 USD, NO SCALING ALLOWED)
  staged_exposure_tiers:
    stage_a_trades: 10
    stage_a_max_order_usd: 25.0
    stage_b_trades: 30
    stage_b_max_order_usd: 50.0
    stage_c_max_order_usd: 100.0  # Maximum order cap (10% of $1,000 account)
  max_single_order_usd: 100.0
  max_concurrent_positions: 2     # Ultra-conservative (1-2 positions)
  max_daily_loss_usd: 20.0        # Stricter 2.0% daily circuit breaker ($20)
  max_weekly_loss_usd: 40.0       # Stricter 4.0% weekly loss limit ($40)
  max_pilot_drawdown_usd: 50.0    # Stricter 5.0% pilot termination limit ($50)
  prohibit_margin: true
  prohibit_shorting: true
  prohibit_options: true
  prohibit_leverage: true
  prohibit_unrelated_assets: true

autonomous_gate_specification:
  per_trade_human_approval: false   # REMOVED FOR PHASE 6A
  manual_session_arming: true       # RETAINED: Mandatory daily operator authorization
  recheck_stale_before_submit: true
  max_quote_staleness_sec: 15.0
  max_allowed_spread_bps: 3.0
  signal_ttl_milliseconds: 3000     # 3.0 second hard signal TTL
  max_decision_latency_ms: 250.0
  prohibit_risk_override: true

model_specification:
  primary_model:
    architecture: "XGBoostClassifier"
    model_sha256_hash: "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
    hyperparameters:
      n_estimators: 100
      max_depth: 3
      learning_rate: 0.05
      subsample: 0.8
      colsample_bytree: 0.8
      random_state: 42
    calibration: "PlattScaling"
    confidence_threshold: 0.58

  meta_label_model:
    architecture: "XGBoostClassifier"
    meta_model_sha256_hash: "c5d2460186f7233c927e7db2dcc703c0e500b653ca82273b7bfad8045d85a470"
    purpose: "Trade Filtering (TAKE_TRADE vs REJECT_TRADE)"
    hyperparameters:
      n_estimators: 50
      max_depth: 2
      learning_rate: 0.05
      random_state: 42
    features:
      - "model_confidence"
      - "expected_move_bps"
      - "realized_volatility_bps"
      - "rvol_14"
      - "spread_bps"
      - "regime_volatility"
      - "vwap_distance_bps"
      - "signal_agreement"
    take_threshold: 0.52

  ranking_model:
    architecture: "CostAwareOpportunityRanker"
    score_formula: "expected_return_bps - estimated_roundtrip_cost_bps - lambda * expected_volatility_bps"
    lambda_risk_penalty: 0.05
    candidate_selection: "Top-1 / Top-3"
    top_k: 3

target_specification:
  primary_horizon: "15m"
  horizon_bars: 3  # 3 x 5-minute bars
  decay_half_life_minutes: 34
  economic_buffer_bps: 5.0

universe_and_archetypes:
  primary_archetype: "HIGH_BETA_HIGH_VOL"
  symbol_allow_list:
    - "NVDA"
    - "AMD"
    - "TSLA"
  benchmark_symbols:
    - "SPY"
    - "QQQ"

exit_rules:
  time_exit_bars: 3  # 15 minutes
  signal_deterioration_exit: true
  stop_loss_pct: 0.015
  take_profit_pct: 0.030
  end_of_day_liquidation: true
  eod_cutoff_time: "15:50:00"
